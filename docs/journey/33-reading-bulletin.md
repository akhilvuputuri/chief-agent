# 33 — Daily reading bulletin: how can a feed learn from explicit votes without a model per edition?

Work date(s): 2026-09-23. Written/revised: 2026-09-23.
Status: implemented and tested; independent review, merge and the operator rollout of migration 019 pending. No feeds, interests or schedule have been configured in production.

## User-visible problem and preceding iteration

[Issue #50](https://github.com/akhilvuputuri/chief-agent/issues/50) asks for five useful readings a day in Telegram, each with Like / Dislike, where later selections improve from that explicit feedback. Interests, sources and delivery time must be obtained from the owner, not assumed.

Preceding iterations: [24 — Scheduled routines](24-scheduled-routines.md) supplied a timer for agent jobs and explicitly left feeds and ranking to domain work; [25 — Stock watchlist](25-stock-watchlist.md) showed that a deterministic in-process monitor with its own outbox fits better when a model call per run is unnecessary and the output needs buttons.

## Evidence

- **Design review (documented)**: a routine occurrence runs the full agent and delivers model prose. That would cost model usage every day, could not attach per-item callbacks to its answer, and could produce a different selection when retried. The bulletin therefore reuses the watchlist pattern — a deterministic tick on the existing 15-second timer and a persisted edition outbox — instead of the routine executor. The owner's issue comment asked domain features to use `routine_*` for owner-selected times; this entry records the deviation and its reason, as journal 25 did.
- **Synthetic checks (tested)**: 36 PGlite/mocked tests (19 initial, 17 review regressions) in `tests/reading.test.ts`: RSS/Atom/CDATA/entity parsing with hostile markup, bidi characters, `javascript:` links, DTD entities and future/missing dates; URL canonicalization; the DNS guard against private, metadata and mapped addresses; explicit-configuration gating, foreground-only mutation and owner scope; edition content (five distinct stories, duplicate cluster collapsed, discovery pick, excerpt/reason/link, callback length); explicit shortfall with a failed feed; owner-bound, replay-safe buttons with change/undo and restart persistence; hard mutes versus weak dislikes; offline ranking fixtures (neutral no-feedback items, likes moving similar candidates, bounded/decaying weights, reason targeting, overrides, diversity caps, labelling); scheduler timezone/day boundaries, DST, latest-only catch-up, database-level duplicate refusal and allowlist; all-feeds-failed retry then disclosure; uncertain delivery never resent; pause muting; override/reset/explain/metrics. These verify implementation logic, not real feeds, Telegram rendering or reading quality.
- **Rollout procedure (tested)**: `scripts/test-deploy-reading.py` runs 13 offline checks (baseline cases extended as releases landed) against `scripts/deploy-reading.py` (exact reviewed baselines, additive-only 019, exact Compose addition, rollback paths). The script differs from the reviewed watchlist script only in baseline, migration name/marker and the absence of environment changes.
- **Development failures (tested)**: the first run failed four tests. Two were real defects: feed-add wrote `last_fetched_at` with database time instead of the injected clock, so later refreshes were wrongly treated as cached; and a per-topic cap blocked source diversity when the owner has a single dominant interest (selection now relaxes the topic cap before the source cap). Vote timestamps and edition creation time now also use the injected clock, which a reset test exposed.

## Diagnosis and alternatives

- **Model summaries vs feed excerpts**: v1 shows the feed's own description, labelled “Feed excerpt”, or says only the headline was available. This is the most grounded option with no model cost; it is not a full-text summary. A cached model summary per selected item remains possible later.
- **Direct feed fetch**: other retrieval goes through hosted providers. RSS must be read as XML repeatedly, so the gateway fetches feeds itself behind a connection-time DNS guard (any non-public answer refuses the connection) plus the existing public-HTTPS hostname check. This is a new egress path and a review focus.
- **Learning rule**: a pure function of current votes (like +1/+0.5, more +2/+1, plain dislike −0.3/−0.15, reasons target topic or source), 30-day half-life, ±3 per key, ±1.5 total effect. Rejected: incremental counters (double counting on replay/undo), model fine-tuning, and blacklisting on dislike. A mute is the only hard exclusion and is always an explicit owner action.
- **Diversity and discovery**: near-duplicate titles cluster (Jaccard ≥ 0.6), at most two per source and topic when alternatives exist, and one discovery slot for non-interest items that are not disliked. Fewer items are delivered rather than padding.
- **Topic matching is lexical** (phrases and owner keywords). Semantic classification would need a model call per candidate; deferred until lexical misses are observed.

## Implementation and review

See [the runbook](../reading-bulletin.md). `db/019_reading.sql` adds settings, feeds, candidates, preference versions, editions (unique per owner-local date for scheduled editions), items, votes and feedback events. `src/reading-feed.ts` parses and fetches; `src/reading.ts` ranks, learns, builds, schedules, delivers and handles buttons; seven `reading_*` tools are offered only when migration 019 is present. `src/telegram.ts` adds the `rd:` callback handler; `src/main.ts` wires the scheduler and outbox behind the migration check; `compose.yaml` lists migration 019.

Automated Devin Review on `4f74683` (23 September) posted four findings, each reproduced by a regression test that fails on that commit and passes after the fix: (1) a scheduled and an on-demand build running concurrently could both select the same candidates — builds are now serialized per owner in the gateway process with the on-demand quota checked inside; (2) removing the last feed left the bulletin enabled, producing empty daily editions — it now switches the bulletin off and mutes pending scheduled editions, and the scheduler ignores owners without an active feed; (3) a delivery time inside a spring-forward gap resolved an hour early (02:30 → 01:30 EST) — it now moves forward by the gap; (4) discovery picks ignored the two-per-domain cap — discovery now applies it unless no alternative exists.

Independent review, round 1 (Claude Opus 5.5 subagent; GPT-6 Astra unavailable in this environment) of `4f74683`: **REQUEST CHANGES**. Blocking: a feed redirect to a target `publicHttps` refuses (HTTPS→HTTP, explicit port, internal name) threw inside the HTTPS response callback — an uncaught exception that would crash the gateway before the feed entered backoff, so the scheduler would re-crash it every tick. The fetcher now separates each request from redirect handling and converts every failure into a rejection; a regression through `PublicFeedFetcher` with a mocked `https.request` fails on `4f74683` with the uncaught exception and passes after. Non-blocking findings fixed with regressions: two plain dislikes removed a non-interest topic from discovery for weeks (discovery floor is now −1); mutes only checked three topics and the headline (now every label and the excerpt); a lone-surrogate entity failed the whole feed's JSON insert; links over 2,000 characters were truncated into broken URLs (now dropped); `<rss` text inside an Atom entry flipped format detection (root element now decides); IPv4-compatible, 6to4, NAT64 local-use and site-local IPv6 ranges are now refused; the trace kept only 15 candidates, which could not replay selection (now a 60 + 20 snapshot); partial deliveries are visible as `items`/`sent` in `reading_status`; `reading_status`/`reading_explain` are read operations; a scheduled build straddling local midnight keeps its trigger date. The reviewer also noted the branch conflicted with main (journal numbering) and the rollout baseline was stale; both are reconciled below. Reconciliation: merged main (`7648fca`, PRs #80/#81), renumbered this entry from 31 to 32 after main's `31-calendar-authorization-failure.md`, and added the two app-only releases to the rollout baselines after checking that neither changed `db/`, `compose.yaml`, `Dockerfile` or `scripts/cloud-release.py`. Round 2 (`beb3f3e`): every code fix verified (including a 44,286-case DST sweep across 11 zones), but **REQUEST CHANGES** — and Devin Review independently flagged the same defect: `5176be3` had committed a `node_modules` symlink from the scratch worktree used for the fixes (`git add -A`; the `node_modules/` ignore rule matches only directories). Release archives must contain only regular files and directories, so both `deploy-reading.py` and the trusted release handler would have refused this and every later main release; the fast-forward also replaced the local checkout's dependency directory with a self-referencing link. `51ad26b` untracks it and ignores `node_modules` as a file or directory; the real `git archive` passes `unpack()`. Lesson: check `git status` for unexpected entries before committing from a worktree with linked dependencies. Round 3 (`d3193a3`): **APPROVE** — symlink gone, real `git archive` accepted by `unpack()`, full checks re-run, `.gitignore` change inert for Docker and the CI npm cache, journal text matched PR history. Main then advanced to the v0.3.23 model-policy release (`7b1cff9`, PRs #82–#86; Dockerfile copies `config/model-policy.json`, no `db/`/Compose/release-handler change), so the branch merged main, moved to version 0.3.24 under the patch-only policy, and added `7b1cff9` to the rollout baselines. Round 4 (`5c07dc7`): **APPROVE**, with a dry run of the real `unpack`/`validate_changes` against a simulated `7b1cff9` host; nits: main's own `32-repo-controlled-model.md` duplicated this entry's number (renumbered to 33), a missing comma and a stale test count. Devin Review on `5c07dc7` then found two defects, each reproduced by a regression that fails on `5c07dc7`: after a learning reset, pressing the same vote again kept its pre-reset timestamp so it could never count again (an unchanged vote is now re-dated when it predates the reset); and a vote, mute or pause committed separately from its audit event, so a failed audit write reported "Nothing changed" after the change had been saved (each is now one statement with its audit row, and a failed keyboard re-read no longer masks a committed mute). Round 5 (`a0b2e5f`): **APPROVE**; the reviewer forced the audit insert to fail mid-statement with a trigger and confirmed nothing was saved. Devin Review on `a0b2e5f` then found four more defects, each with a regression that fails on `a0b2e5f`: `reading_preferences` reset/override still committed separately from its audit row (now one statement); pausing through `reading_settings` committed before pending editions were muted, so a delivery claim in between could still send (settings and mute are now one statement, and the claim itself requires an enabled, unpaused owner for scheduled editions); paired Atom `<category term="…"></category>` elements lost their term (all category tags are now read, `term` first, deduplicated); and diversity caps can select candidates below rank 60, which the trace snapshot then omitted (both rankings' selections are now always stored, and the runbook no longer claims exact replay of the greedy scan). Round 6 (`e3bfa52`): **APPROVE**, with two wording nits (fixed here: the runbook no longer says a pause can never be overtaken — a claim that committed first still delivers — and regressions are described as failing on the commit before their fix). Devin Review on `e3bfa52` then found two provenance defects, each with a regression that fails on `e3bfa52`: a preference version was reused whenever weights matched, so it could name votes that no longer produced them (the contributing vote set is now part of version identity, compared order-stably because jsonb reorders keys); and a syndicated URL switched to the newer feed while keeping the older feed's excerpt (the displayed fields and attribution now always move together as one feed's tuple). Round 7 (`51ce9d5`): **APPROVE**, with the multi-column `ON CONFLICT DO UPDATE SET (…) = (SELECT …)` confirmed for Postgres 17 and an adoption matrix for syndicated URLs. Devin Review on `51ce9d5` then found three more defects, each with a regression that fails on `51ce9d5`: removing the last feed still deleted, disabled and muted in separate statements, leaving a window for a delivery claim (now one statement; its "no other active feed" test excludes the deleted id because all parts share the pre-delete snapshot); three-letter language codes such as `eng` never matched a feed declaring `en-US` (codes are now normalized to one canonical primary subtag on save and comparison); and prefixed Atom documents (`<atom:feed>`, `<atom:entry>`) parsed as empty (the root's prefix is now stripped from every tag before extraction). Round 8 pending.

## Verification and outcome

- Tested 23 September 2026 on the first head `4f74683`: `npm run check` passed (362 application + 10 script tests); rollout tests 12 passed; CI `test` passed. After review fixes and the main merge: `npm run check` passed (374 application tests including 27 reading tests, 10 script tests; 377 after merging v0.3.23, 378 with the round-4 Devin regression, 382 with the round-5 Devin regressions, 384 with the round-6 Devin regressions, 386 with the round-7 Devin regressions); `python3 scripts/test-deploy-reading.py` 13 passed; `npm run format:check` clean. Each review regression was confirmed to fail on the commit before its fix. Package version 0.3.24 per the patch-only policy after main released 0.3.23 (candidate; not a release).
- Not verified: real feeds, delivery time on the phone, button rendering, excerpt quality, and whether votes improve perceived usefulness. Like rate and rating coverage start accumulating only after activation; a few votes are not evidence of improvement.

### Release closure — pending

Requires independent approval of the exact PR head, merge, the reviewed operator rollout of migration 019 from a reviewed baseline (currently live `7648fca`) and verification of release SHA, health and migration marker. The owner then configures interests, feeds, time and timezone; first acceptance is an on-demand preview followed by one scheduled edition.

## Follow-up and next iteration

- Compare `inBaseline` picks against feedback-only picks once enough items are rated.
- Consider optional cached model summaries and semantic topic tagging if feed excerpts or lexical matching prove weak.
- Public search as a candidate source and plugin packaging remain deferred.
