# Historical Hermes adapter

Reference and optional benchmark material from the original prototype. Production Compose does not build or call this service. The internal HTTP callback it used no longer exists in the application. Do not start this bridge against production.

The pinned upstream version and attribution remain in the Dockerfile. Optional Python tests and the synthetic bridge smoke are historical comparison tools, not launch dependencies of the custom runtime.
